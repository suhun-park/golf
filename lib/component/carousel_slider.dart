import 'package:flutter/material.dart';

class CusttomCarouselSlider extends StatelessWidget {
  const CusttomCarouselSlider({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
